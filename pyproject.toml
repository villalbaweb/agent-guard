[project]
name = "agent-guard"
version = "0.1.0"
description = "A policy-driven agentic operating system."
dependencies = [
    "langchain>=0.3.0",
    "langchain-anthropic",
    "langchain-google-vertexai",
    "langchain-google-genai",
    "langchain-openai",
    "python-dotenv",
    "langgraph",
]
requires-python = ">=3.13"

[tool.uv]
package = false
